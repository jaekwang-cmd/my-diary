# 설치 안내서

순서대로 따라 하시면 됩니다. **1단계**만 끝내도 폰에서 앱으로 쓸 수 있고,
**2단계**를 하면 구글 드라이브 백업까지 됩니다.

---

## 0. 지금 바로 PC에서 미리 보기

```bash
python -m http.server 5173 --directory C:\Users\mj497\my-diary
```

브라우저에서 `http://localhost:5173` 접속.

---

## 1단계. GitHub Pages에 올려서 폰에서 쓰기

### 1-1. GitHub 계정 만들기

[github.com](https://github.com) 에서 가입합니다. (무료)

### 1-2. 새 저장소 만들기

1. 오른쪽 위 **+** → **New repository**
2. **Repository name**: `my-diary`
3. **Public** 선택 (Private이면 GitHub Pages 무료 사용 불가)
4. README 체크박스는 **끄고** → **Create repository**

> Public이라 소스코드는 공개되지만, **일기 내용은 절대 올라가지 않습니다.**
> 일기는 폰 안과 본인 구글 드라이브에만 저장됩니다.

### 1-3. 코드 올리기

`my-diary` 폴더에서 아래를 실행하세요. `본인아이디` 두 군데를 실제 GitHub 아이디로 바꿔야 합니다.

```bash
cd C:\Users\mj497\my-diary
git init
git add .
git commit -m "추억 일기 PWA"
git branch -M main
git remote add origin https://github.com/본인아이디/my-diary.git
git push -u origin main
```

처음 push할 때 로그인 창이 뜨면 GitHub 계정으로 로그인하면 됩니다.

### 1-4. Pages 켜기

1. 저장소 페이지 → 위쪽 **Settings**
2. 왼쪽 메뉴 **Pages**
3. **Source**: `Deploy from a branch`
4. **Branch**: `main` / `/ (root)` → **Save**
5. 1~3분 기다리면 주소가 나옵니다:

```
https://본인아이디.github.io/my-diary/
```

**이 주소를 꼭 메모해 두세요.** 2단계에서 씁니다.

### 1-5. 폰에 설치하기

**아이폰 (반드시 Safari로 열어야 합니다)**
1. Safari에서 위 주소 접속
2. 아래 **공유 버튼**(□↑) 누르기
3. **홈 화면에 추가** → **추가**

**안드로이드 (Chrome)**
1. Chrome에서 위 주소 접속
2. 오른쪽 위 **⋮** 메뉴
3. **앱 설치** 또는 **홈 화면에 추가**

이제 홈 화면 아이콘으로 실행하면 주소창 없이 앱처럼 뜹니다.

### 아이폰에서 알아둘 것

**기능은 안드로이드와 똑같이 전부 됩니다.** 일기·사진·달력·루틴·D-DAY·통계·검색·잠금·구글 드라이브 전부요.
다만 아이폰 특성상 두 가지만 다릅니다.

| 항목 | 아이폰에서는 |
|---|---|
| 설치 | **반드시 Safari로** 열어서 추가해야 합니다. Chrome으로 추가하면 앱처럼 안 뜹니다. |
| 알림 | **홈 화면에 추가한 뒤에만** 켜집니다. 그리고 앱을 한 번씩 열어야 알림이 뜹니다. |

그리고 아이폰 Safari는 **오래 안 쓴 웹앱의 저장 데이터를 정리할 수 있습니다.**
홈 화면에 추가하면 대부분 괜찮지만, 확실하게 하려면 **설정 → 자동 백업을 켜두세요.**
설정 화면의 `저장소 보호` 항목이 **보호됨** 으로 나오면 안전한 상태입니다.

---

## 2단계. 구글 드라이브 연동

무료이고, 심사도 필요 없습니다. 10분 정도 걸립니다.

### 2-1. 구글 클라우드 프로젝트 만들기

1. [console.cloud.google.com](https://console.cloud.google.com) 접속 (구글 계정으로 로그인)
2. 화면 위쪽 프로젝트 선택 드롭다운 → **새 프로젝트**
3. 이름 아무거나 (예: `my-diary`) → **만들기**
4. 만든 프로젝트가 선택되어 있는지 확인

### 2-2. Drive API 켜기

1. 왼쪽 메뉴 **API 및 서비스** → **라이브러리**
2. `Google Drive API` 검색 → 클릭 → **사용** 버튼

### 2-3. 동의 화면 설정

왼쪽 메뉴에서 **Google Auth 플랫폼**(예전 이름: OAuth 동의 화면)으로 들어갑니다.

1. **시작하기** 를 누르고 순서대로 입력
   - **앱 이름**: `추억 일기`
   - **사용자 지원 이메일**: 본인 이메일 선택
   - **대상**(Audience): **외부** 선택
   - **연락처 정보**: 본인 이메일 입력
2. 동의하고 **만들기**

### 2-4. 게시 상태를 "프로덕션"으로

**대상**(Audience) 탭에서 **앱 게시** 또는 **프로덕션으로 푸시** 버튼을 누릅니다.

> **왜 필요한가**: 테스트 모드로 두면 로그인이 주기적으로 풀립니다.
> 이 앱은 민감하지 않은 권한(`drive.file` — 앱이 만든 파일만 접근)만 쓰기 때문에
> 프로덕션으로 전환해도 **구글의 별도 검증 절차가 필요 없습니다.**
>
> 만약 게시 버튼이 안 보이거나 검증을 요구하면, 그냥 **테스트** 상태로 두고
> **테스트 사용자**에 본인 Gmail 주소를 추가해도 됩니다. (가끔 재로그인만 하면 됩니다)

### 2-5. 클라이언트 ID 발급

1. **클라이언트**(Clients) 탭 → **클라이언트 만들기**
   - 예전 UI라면: **API 및 서비스** → **사용자 인증 정보** → **사용자 인증 정보 만들기** → **OAuth 클라이언트 ID**
2. **애플리케이션 유형**: `웹 애플리케이션`
3. **이름**: 아무거나 (예: `일기장 웹`)
4. **승인된 JavaScript 원본** — **+ URI 추가** 를 눌러 아래 두 개를 등록:

   ```
   https://본인아이디.github.io
   http://localhost:5173
   ```

   > 주소 뒤에 `/my-diary` 같은 경로를 붙이면 **안 됩니다.** 도메인까지만 넣으세요.
   > 맨 끝에 `/` 도 붙이지 마세요.

5. **승인된 리디렉션 URI** 는 **비워 두세요.** (이 앱은 필요 없습니다)
6. **만들기** → 나타난 **클라이언트 ID** 복사
   (`123456789-xxxxx.apps.googleusercontent.com` 형태)

### 2-6. 앱에 입력하기

**방법 A — 앱에서 바로 입력 (간편)**
1. 일기장 앱 → **설정** 탭
2. **구글 드라이브** → **구글 클라이언트 ID 입력하기**
3. 복사한 값 붙여넣기 → **저장**
4. **구글 계정 연결하기** → 구글 로그인

**방법 B — 파일에 넣고 배포 (기기마다 다시 입력할 필요 없음)**

`js/config.js` 를 열어 따옴표 안에 붙여넣습니다.

```js
export const DEFAULT_CLIENT_ID = '123456789-xxxxx.apps.googleusercontent.com';
```

저장한 뒤 다시 push:

```bash
cd C:\Users\mj497\my-diary
git add .
git commit -m "구글 클라이언트 ID 설정"
git push
```

### 2-7. 확인

설정 → **지금 백업하기** 를 누르면 구글 드라이브에
**`추억일기 백업`** 폴더가 생기고 그 안에 데이터가 저장됩니다.

다른 기기(아이폰 등)에서 같은 주소로 접속 → 같은 구글 계정 연결 →
**드라이브에서 복원** 하면 일기가 넘어옵니다.

**자동 백업**을 켜두면 일기를 저장할 때마다 알아서 올라갑니다.

---

## 로그인이 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| `redirect_uri_mismatch` / `origin` 오류 | 2-5의 **승인된 JavaScript 원본** 주소가 실제 접속 주소와 다릅니다. `https://아이디.github.io` 처럼 도메인까지만, 끝에 `/` 없이 넣으세요. |
| 팝업이 바로 닫힘 | 브라우저 팝업 차단. 주소창의 차단 아이콘에서 허용하세요. |
| `앱이 차단됨` / `확인되지 않은 앱` | 2-4를 안 했거나, 테스트 사용자에 본인 계정이 없습니다. |
| 설정 변경 직후 그대로임 | 구글 설정 반영에 몇 분 걸립니다. 5분 뒤 다시 시도하세요. |

---

## 나중에 코드를 고쳤을 때

```bash
cd C:\Users\mj497\my-diary
git add .
git commit -m "수정"
git push
```

push 후 1~2분이면 반영됩니다. 폰에서 바뀐 게 안 보이면
앱을 완전히 껐다 켜거나, 브라우저에서 새로고침하세요.
